import CarRow from './CarRow.jsx';

export default function CarTableBody() {
    // For now, render 2 placeholder rows to visualize structure
    return (
        <tbody>
        <CarRow />
        <CarRow />
        </tbody>
    );
}
